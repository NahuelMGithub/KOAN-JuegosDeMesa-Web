---------------------- Una vez por proyecto

npn init    Inicia el npn y crea el package.json hay que ir poniendo enter y terminamos con Y

npn install -g sass    

------------------------

Cada vez que quiero que copie:

sass --watch archivo destino
ej:
sass --watch sass/style.scss css/style.css